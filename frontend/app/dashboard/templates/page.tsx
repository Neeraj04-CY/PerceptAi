// Business Templates — the canonical route for the department catalog.
// The catalog component lives in the studio namespace (which also owns the
// role editor at /dashboard/studio/[id]); this route is what the nav links.
export { default } from "../studio/page";
